/* This file is unused — the licensing model moved from Stripe/license
 * keys to admin-managed username/password accounts. See auth.js and
 * index.js. Kept only so an old reference doesn't 404; safe to delete. */
module.exports = {};
