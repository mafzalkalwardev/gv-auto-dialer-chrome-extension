/* ------------------------------------------------------------------
 * config.js — the one place that points the extension at your
 * accounts server. Update this after you deploy the server in
 * /server (see server/README.md), then re-zip and re-upload to the
 * Chrome Web Store.
 * ------------------------------------------------------------------ */

export const ACCOUNTS_API_BASE = 'https://gv-auto-dialer-accounts.vercel.app';
